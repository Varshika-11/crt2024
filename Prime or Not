#include <stdio.h>

int main(){
    int i,n,c;
    scanf("%d",&n);
    for(i=1;i<=10;i++)
    {
        if(n%i==0)
        c=c+1;
    }
    if(c==2)
    {
        printf("prime");
    }
    else
      printf("Not a prime");
    return 0;
}
